<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <template id="vuente_website_slides_all_prompted" name="Slides All Promoted" inherit_id="website_slides.home">
            <xpath expr="//section[2]" position="replace">
                <!-- Prevent slide appearing twice -->
            </xpath>

            <xpath expr="//section[2]" position="replace">


                        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 mb16" t-if="not slide_type and category_datas and not tag">
                            <t t-foreach="category_datas" t-as="category">
                                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 mt8 oe_slides_bottom_border">
                                        <a t-if="category['id']" t-attf-href="/slides/#{slug(channel)}/category/#{category['id']}" class="h1 mt0 mb0">
                                            <t t-esc="category['name']"/>
                                        </a>
                                        <span t-if="not category['id']" title="Uncategorized presentation" class="h1 mt0 mb0">
                                            <t t-esc="category['name']"/>
                                        </span>
                                        <a t-attf-href="/slides/#{slug(channel)}/category/#{category['id']}" class="pull-right" t-if="category['total'] - 4 &gt; 0 and category['id']">
                                            <i class="fa fa-arrow-right"/> See all
                                        </a>
                                    </div>
                                </div>
<br/>




<t t-set="slides" t-value="category['slides']"/>

            <section class="oe_slides_promote_box mt32 mb32" t-foreach="slides" t-as="slide">
                <div class="container">
                    <div class="row">
                        <div class="col-md-5 mt16 mb16">
                                <h3><a t-attf-href="/slides/slide/#{slug(slide)}">
                                    <t t-esc="slide.name"/>
                                </a></h3>

<a t-attf-href="/slides/slide/#{slug(slide)}">
                                <img t-attf-src="/web/image/slide.slide/#{slide.id}/image_medium" class="img-responsive shadow oe_slides_promote_image"/>
                            </a>
                        </div>
                        <div class="col-md-7 mt16 mb16">
                            <h3 class="row"><!--
                                <a t-attf-href="/slides/slide/#{slug(slide)}">
                                    <t t-esc="slide.name"/>
                                </a>-->
                            </h3>
                            <br/>
                            <div class="row mt8 mb8" t-if="slide.tag_ids">
                                <t t-foreach="slide.tag_ids" t-as="slide_tag">
                                    <a t-attf-href="/slides/#{slug(channel)}/tag/#{slug(slide_tag)}" t-attf-class="label label-default #{tag and tag.id == slide_tag.id and 'label-primary' ''}" t-field="slide_tag.name"/>
                                </t>
                            </div>
                            <div class="row oe_no_empty" t-esc="slide.description"/>
                            <p class="row mt8">
                                <b>Share:</b>
                                <t t-call="website_slides.slides_share">
                                    <!--<t t-set="slide" t-value="channel.promoted_slide_id"/>-->
                                </t>
                            </p>
                        </div>
                    </div>
                </div>

            </section>









                            </t>
                        </div>



            </xpath>
            </template>

    </data>
</openerp>
